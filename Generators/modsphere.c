#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){

	int nums[10];
	char in[256],out[256];
	FILE *f,*g;

	f = fopen(in,"r");
	g = fopen(out,"w");

	while(fscanf(f,%s,search!=){
		if(strcomp(search,"ATOM"==0){
			fscanf(f, %	
		}
	}
	

}
